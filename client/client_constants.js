const Message  = {
    CONNECTION : "connection",
    DISCONNECT : "disconnect",
    WORLD : "world",//{x, y, z, sx, sy, sz}
    STRETCH_REQUEST : "stretch_request",
    PLAYER : "player",//{x, y, x}
    
};